import React from 'react';
interface FilterProspectProps {
    windowId: string;

}

const FilterModel: React.FC<FilterProspectProps> = ({ windowId }) => {
    return (
        <p>Filter</p>
    )
}

export default FilterModel;